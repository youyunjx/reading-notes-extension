node_modules/
dist/
*.log
.DS_Store
.vite/

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Extension signing key — keep private, never commit
.keys/

# Personal data / recovery artifacts — never commit (repo is public)
.recovery/
jot-backup*.json
reading-notes-backup*.json

# Web Store packaging artifacts
.package/
jot-v*.zip
reading-notes-v*.zip
